name: Format

on:
  workflow_call:

jobs:
  format:
    runs-on: ubuntu-latest

    steps:
      - name: Run Ruff Formatting
        run: |
          poetry run ruff format --config pyproject.toml src/
